### Явное
Приведение типов (typecast)
```c
(тип)выражение


int i = 1;
float f = (float)i / 2; // 0.5f
```

### Неявное

Автоматически производится в выражениях. Допускает потерю информации
```c
float f = 0.5f;
int i = f;
```
